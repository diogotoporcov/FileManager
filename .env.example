POSTGRES_DB=file-manager
POSTGRES_USER=file-manager
# GENERATE A BASE64 TOKEN WITH:
# openssl rand -base64 32
POSTGRES_PASSWORD=REPLACE-ME!

RABBITMQ_USERNAME=file-manager
# GENERATE A BASE64 TOKEN WITH:
# openssl rand -base64 32
RABBITMQ_PASSWORD=REPLACE-ME!

# GENERATE A BASE64 TOKEN WITH:
# openssl rand -base64 64
JWT_SECRET=REPLACE-ME!

JWT_ISSUER=auth-service
JWT_AUDIENCE=file-manager

SERVER_PORT=8080
