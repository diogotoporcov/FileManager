# PostgreSQL
POSTGRES_DB=filemanager
POSTGRES_USER=filemanager
POSTGRES_PASSWORD=replace_me:urlsafe:24
POSTGRES_HOST=filemanager-postgres
POSTGRES_PORT=5432

# MinIO / S3
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=replace_me:urlsafe:24
MINIO_ENDPOINT=http://localhost:9000
MINIO_BUCKET_NAME=filemanager
# Worker also supports S3_ACCESS_KEY, S3_SECRET_KEY, S3_ENDPOINT, S3_BUCKET_NAME aliases

# Redpanda / Kafka
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
FILE_PROCESSING_REQUESTED_TOPIC=file.processing.requested

# Keycloak
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=replace_me:urlsafe:24
KEYCLOAK_ISSUER_URI=http://localhost:8082/realms/filemanager

# Duplicate Detection
PHASH_THRESHOLD=10

# Service Ports (External Host Access)
PORT_POSTGRES=5432
PORT_MINIO=9000
PORT_MINIO_CONSOLE=9001
PORT_REDPANDA=9092
PORT_KEYCLOAK=8082
PORT_API=8081
PORT_GATEWAY=8080

# Internal API
INTERNAL_API_TOKEN=replace_me:hex:64

# Gateway Safety
GATEWAY_MAX_REQUEST_SIZE=100MB
GATEWAY_CONNECT_TIMEOUT=5000
GATEWAY_RESPONSE_TIMEOUT=60s

# Metrics
METRICS_ENABLED=true
